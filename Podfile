project 'ReferralLaboratories.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReferralLaboratories' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReferralLaboratories

  target 'ReferralLaboratoriesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ReferralLaboratoriesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
